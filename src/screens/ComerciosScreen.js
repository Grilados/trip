import React, { Component } from 'react';
import { Text } from 'react-native';

export default class ComerciosScreen extends Component {
    render() {
        return(
            <Text>ComerciosScreen</Text>
        );
    }
}